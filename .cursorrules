# Instructions

// ... existing code ...

# Lessons

// ... existing code ...

## Web UI Prototype Lessons from MC Markets

- **步驟導航控制問題**: 務必實現嚴格的步驟導航控制，防止用戶跳過必須完成的步驟，使用 `completedSteps` 數組追蹤進度並在 `goToStep` 函數中添加條件檢查。
- **視覺反饋一致性**: 確保所有可選元素（如監管選項、賬戶類型）有明確的視覺效果（如邊框顏色變化）來表示已選狀態，使用 `.selected` 類統一處理選中效果。
- **已完成步驟的返回功能**: 當步驟指示器顯示綠色勾號（已完成）時，需保持其可點擊性，確保用戶能夠返回修改前面的步驟。
- **背景色強制覆蓋**: 對於設計要求特定背景色的頁面，使用多層手段確保顏色一致：
  - 在 CSS 變量設定基本顏色
  - 使用 `!important` 提高優先級
  - 添加固定定位的背景覆蓋層
  - 為每個主要容器單獨設置inline樣式
- **地區相關限制邏輯**: 實現基於用戶地區的限制邏輯（如歐盟居民不能選擇FSA監管）時，需結合前端限制和清晰的用戶提示，確保體驗流暢且合規。
- **表單數據本地存儲**: 使用 localStorage 存儲用戶填寫的數據，避免頁面切換時丟失信息，特別是在多步驟表單中。
- **進度指示器更新機制**: 進度指示器需要有完整的更新機制，包括：
  - 當前步驟高亮
  - 已完成步驟顯示勾號
  - 進度線顏色變化
  - 步驟文字顏色變化
- **錯誤防禦編程**: 所有用戶交互函數需添加錯誤檢查和日誌輸出，使用 console.log 記錄關鍵步驟，便於問題排查。
- **響應式設計考量**: 確保進度指示器等關鍵UI元素在不同屏幕尺寸下有合適的佈局調整，使用媒體查詢處理移動端顯示。
- **內聯樣式與類樣式結合**: 對於需要強制覆蓋的樣式使用內聯樣式+!important，對於需要動態切換的狀態樣式則使用類名切換。

## 版本控制與部署教訓 (第二版開發過程)

- **Git 倉庫結構規劃**: 在項目開始前先規劃好文件結構，避免將臨時文件和備份文件（如 `.tmp`、`.bak`）提交至倉庫，應在 `.gitignore` 中明確排除這些文件。
- **提交前檢查**: 使用 `git status` 和 `git diff --staged` 仔細檢查將要提交的文件，確保不包含敏感信息和不必要的臨時文件。
- **分支管理策略**: 針對不同功能和版本創建獨立分支，避免直接在 main 分支上進行大量修改，使用 `git checkout -b feature-name` 創建新功能分支。
- **Vercel 部署準備**: 
  - 確保項目根目錄包含完整的靜態網站結構（`index.html`、`css/`、`js/` 目錄）
  - 創建 `vercel.json` 文件配置部署參數，包括：
    - 項目名稱和版本
    - 安全配置（HTTP 頭部設置）
    - GitHub 自動部署選項
    - URL 清理和格式設置
- **Vercel CLI 權限問題**: 使用 Vercel CLI 時可能遇到權限問題，解決方案：
  - 使用 `sudo npm install -g vercel` (需要輸入密碼)
  - 或優先選擇 GitHub 連接部署方式，避免權限問題
- **部署後檢查**: 部署完成後，應全面檢查網站功能，特別是：
  - 動態交互功能是否正常
  - 表單提交和數據存儲是否正常
  - 跨設備顯示是否正常
  - 404 頁面是否配置正確
- **網站性能優化**: 部署前檢查並優化：
  - 壓縮圖片和 CSS/JS 文件
  - 確保字體文件使用適當的格式和加載策略
  - 考慮使用 CDN 加速靜態資源加載
- **本地與生產環境差異**: 注意本地開發和生產環境的差異，特別是：
  - 相對路徑與絕對路徑的使用
  - API 端點配置
  - 環境變量的使用

## 多頁面網站整合教訓

- **單一大文件風險**: 嘗試將多個頁面整合到單一文件 (`all-in-one.html`) 導致代碼錯亂和維護困難，特別是當文件超過500行時。
- **模塊化原則**: 應堅持模塊化開發，每個主要功能或頁面使用獨立文件，利用 include 或 components 機制實現代碼複用。
- **備份策略**: 在重大更改前確保有完整備份，標記清晰的備份文件夾（如加上 "never touch" 標記），避免自動化工具意外修改這些文件。
- **CSS隔離**: 不同頁面的樣式應該有明確的作用域限制，防止跨頁面的樣式污染，如使用不同的類名前綴或模塊化CSS。
- **腳本交互邊界**: 當多個頁面整合到同一文件時，頁面間的JavaScript交互極易產生衝突，應嚴格定義作用域和事件處理邊界。
- **選擇器衝突**: 在單一大文件中，DOM選擇器很容易發生重名和衝突問題，應使用頁面特定前綴避免此類問題。
- **文件大小影響性能**: 過大的HTML文件會直接影響首次加載性能和頁面渲染速度，特別是在移動設備上。

## 路徑和環境一致性教訓

- **路徑引用一致性問題**: 在不同環境（本地開發、測試、生產）間移動HTML文件時，確保使用相對路徑而非絕對路徑引用資源和頁面跳轉，避免 `/Users/username/path/to/file` 這類硬編碼路徑。
- **資源文件本地化**: 對於字體、圖標等關鍵資源，優先使用CDN但必須提供本地備份（如使用`onerror`處理CDN加載失敗的情況），確保在任何網絡環境下都能正常顯示。
- **版本標記重要性**: 為每個HTML頁面添加明確的版本信息和更新日期，這在多人協作和故障排查時非常有價值，建議在文件底部或控制台日誌中記錄。
- **跨頁面狀態保持**: 使用`sessionStorage`或`localStorage`時要考慮不同瀏覽器和隱私模式的兼容性，關鍵數據需有備用存儲機制（如URL參數）。
- **開發環境與實際使用環境的差異**: 開發環境中的文件和用戶實際使用環境可能存在差異，特別是文件位置和訪問方式（file://與http://），導致功能無法正常工作。
- **自適應檢測機制**: 實現自動檢測當前執行環境的機制（如通過檢測`window.location`），根據不同環境自動調整資源引用路徑和功能實現。
- **錯誤假設的成本**: 假設用戶使用的文件與開發環境相同可能導致嚴重問題，應該在每個功能實現中添加充分的錯誤處理和自檢機制。
- **頁面加載完成檢查**: 使用`DOMContentLoaded`事件確保DOM完全加載後再執行JavaScript，避免在元素未完全加載時進行DOM操作導致的錯誤。
- **調試信息的重要性**: 在關鍵功能執行點添加`console.log`語句，記錄當前狀態、變量值和環境信息，便於在不同環境中排查問題。
- **功能測試的全面性**: 每個功能實現後需在多種環境下測試（不同瀏覽器、不同路徑、不同加載方式），確保功能的健壯性和可靠性。

## 用戶體驗與交互設計教訓

- **直接跳轉與流程完整性平衡**: 在設計用戶註冊流程時，需要平衡完整流程體驗與便捷操作的需求。提供直接跳轉選項可以提高效率，但也可能導致用戶跳過重要步驟。
- **測試工具的用戶友好性**: 開發測試工具時，應注重直觀性和自解釋性，通過視覺提示（如顏色編碼按鈕）和明確的功能描述幫助用戶理解每個操作的目的和效果。
- **sessionStorage狀態顯示**: 實時顯示當前會話存儲狀態能極大提高測試效率，讓開發者和測試人員直觀了解系統狀態，避免因數據不可見導致的困惑。
- **版本更新日誌**: 在測試工具中嵌入版本信息和更新日誌，確保所有使用者了解最新變更和功能改進，避免使用過時的功能或重複報告已解決的問題。
- **路徑錯誤的用戶提示**: 當檢測到可能的路徑問題時，應該提供明確的錯誤提示和解決建議，而非默默失敗，幫助用戶理解問題本質並採取正確的修復措施。

## 本地HTML文件導航與路徑處理教訓

- **file://協議的特殊性**: 使用file://協議直接開啟HTML文件時，瀏覽器對路徑處理的方式與http://協議有顯著差異，特別是相對路徑的解析和安全限制。
- **動態路徑檢測與處理**: 在多端測試環境中，應實現動態檢測當前路徑的邏輯，根據不同路徑模式（如桌面直接開啟、項目目錄內開啟等）採用不同的路徑處理策略。
- **桌面路徑特殊處理**: 當檢測到用戶直接從桌面或特定路徑（如`/Users/username/Desktop/`）開啟文件時，需有專門的處理邏輯，確保導航能正確跳轉到相應文件。
- **全面日誌記錄**: 在導航函數中添加詳細的日誌記錄，記錄路徑解析的每個環節，包括原始路徑、解析後的目標路徑和決策分支點，便於排查路徑問題。
- **錯誤處理與用戶反饋**: 導航函數應包含完善的錯誤處理機制，當出現路徑解析錯誤時，不僅記錄詳細錯誤日誌，還應向用戶提供清晰的錯誤提示和可能的解決方案。
- **目錄層級檢測**: 實現檢測當前文件所在目錄層級的功能，區分是在項目根目錄、子目錄還是完全外部路徑，據此調整資源引用和頁面跳轉的路徑策略。
- **多層路徑備選機制**: 為同一目標頁面設置多層次的路徑備選方案，依次嘗試不同的路徑模式，提高在各種環境下正確導航的成功率。
- **用戶特定路徑適配**: 針對特定用戶的路徑模式（如包含特定用戶名的路徑`/Users/seanchou/`）提供定制化的路徑處理邏輯，提高特定環境下的兼容性。
- **環境標識機制**: 在頁面加載時判斷當前運行環境（本地文件、開發服務器、生產環境等），並設置全局環境標識變量，使所有導航和資源加載函數能根據環境作出相應調整。
- **路徑測試覆蓋**: 在開發測試階段，確保覆蓋所有可能的文件訪問路徑進行測試，包括相對路徑、絕對路徑、不同目錄層級的訪問以及不同操作系統的路徑格式。
- **跨平台路徑兼容**: 考慮Windows系統（使用反斜杠`\`）和macOS/Linux系統（使用正斜杠`/`）之間的路徑差異，實現能適應不同操作系統的路徑處理邏輯。
- **桌面平台下HTML鏈接問題**: 當用戶直接在桌面打開HTML文件時，文件間的導航特別容易出錯，需要採用完全絕對路徑（`file:///Users/username/Desktop/filename.html`）而非相對路徑，避免導航失敗。
- **多文件一致性更新原則**: 當修改一個頁面的導航邏輯後，應確保所有相關頁面使用相同的導航函數，避免不同頁面間的路徑處理邏輯不一致導致的跳轉問題。
- **共用導航邏輯模塊**: 考慮將導航邏輯提取為獨立的JavaScript模塊，如 `navigation.js`，由所有頁面引用，確保路徑處理邏輯的一致性和可維護性。
- **中心化入口推薦**: 在本地文件測試環境中，推薦設置一個中央入口頁面（如test_all.html），包含所有頁面的跳轉邏輯，並告知用戶優先從該頁面進入應用，避免直接打開獨立頁面時的路徑問題。
- **Desktop文件夾特殊性**: 特別處理路徑中包含"Desktop"或"desktop"的情況，這些路徑通常表示用戶直接在桌面上打開文件，需要特殊的路徑解析策略。
- **硬編碼後備路徑**: 在所有其他路徑解析方法都失敗的情況下，提供硬編碼的後備路徑（如`file:///Users/username/Desktop/`），即使不優雅也能作為最後的保障確保頁面導航不會完全失敗。
- **嘗試-回退策略**: 實現嘗試多種路徑並根據成功與否決定是否回退到下一個備選方案的機制，從最理想的路徑（如相對路徑）開始嘗試，逐步降級到更保守但更可靠的方案（如硬編碼絕對路徑）。

## 文件同步與維護教訓

- **多文件同步更新**: 當項目中存在多個位置的相同功能文件（如同時存在於項目目錄和桌面的相同HTML文件）時，在修改一個文件後，必須立即同步更新所有相關文件，避免版本不一致導致的功能差異和用戶體驗不同。
- **固定同步清單**: 為專案建立一個明確的"文件同步清單"，列出所有需要保持同步的文件路徑，每次修改後對照清單進行全面更新，確保不遺漏任何文件。
- **同步提醒機制**: 在修改關鍵功能文件時，為自己設置提示或檢查點（如修改完成後的檢查步驟），確保想起同步更新所有相關文件。
- **相同功能集中化**: 儘可能避免維護多個相同功能的文件副本，優先考慮使用集中式管理和動態引用方式（如將共用功能放在單一文件中，由其他文件引用）減少同步維護的負擔。
- **測試覆蓋多文件**: 在進行功能測試時，確保測試覆蓋所有存在該功能的文件版本，不僅測試主項目目錄中的文件，也包括桌面或其他位置的相同功能文件。

# Scratchpad 